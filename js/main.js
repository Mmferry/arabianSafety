var lateastCorusesSlider = new Swiper('.lateastCorusesSlider .swiper-container', {

});

var albumsSlider = new Swiper('.albumsSlider .swiper-container', {
	nextButton: '.albumsSlider-button-next',
    prevButton: '.albumsSlider-button-prev'
});

new WOW().init();